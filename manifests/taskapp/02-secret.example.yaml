# Do not commit the real 02-secret.yaml.
# Prefer scripts/create-taskapp-secret.sh instead.
apiVersion: v1
kind: Secret
metadata:
  name: taskapp-secret
  namespace: taskapp
type: Opaque
stringData:
  DATABASE_PASSWORD: "replace-me"
  SECRET_KEY: "replace-me"
